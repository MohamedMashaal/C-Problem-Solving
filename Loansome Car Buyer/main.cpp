#include <iostream>
#include <cstdio>
#include <vector>
using namespace std;

int main()
{
    float maxMonths , monthly , totally , numK ;
    float monthNum , percent ;
    pair<int , int > x ;
    vector <pair<int , int>> z ;
    printf("%d %d",x.first,x.second);
    while(scanf("%f %f %f %f",maxMonths,monthly,totally,numK) , maxMonths > 0){
        for(int i = 0 ; i < maxMonths ; i ++)
            z.b

        for(int i = 0 ; i < maxMonths ; i ++){

        }
    }
    return 0;
}
